apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: skaffold-project-ingress
  labels:
    app.kubernetes.io/name: skaffold-project-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: core-service
                port:
                  number: 80
    - http:
        paths:
          - pathType: Prefix
            path: "/api/notification"
            backend:
              service:
                name: notification-service
                port:
                  number: 80
